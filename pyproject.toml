[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint.per-file-ignores]
# Qt UI Compiler가 생성하며, 다시 생성하면 import 목록도 함께 덮어써집니다.
"mypackage/ex_gui.py" = ["F401"]
